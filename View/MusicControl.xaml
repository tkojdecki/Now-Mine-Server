<UserControl x:Class="NowMine.View.MusicControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MouseDoubleClick="MusicControl_OnMouseDoubleClick">
    <Grid Background="White" Margin="8, 4, 8, 4">

        <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Stretch">

            <Image x:Name ="imgMain" Source ="{Binding Path=Image}" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>

            <StackPanel Orientation="Vertical"
                                VerticalAlignment="Stretch">
                <Label x:Name="lblTitle" Content="{Binding Path=Title}" VerticalAlignment="Top" ClipToBounds="True"/>
            </StackPanel>

        </StackPanel>

        <Border x:Name="border" BorderBrush="Black" BorderThickness="3">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="16" Direction="-100" x:Name="dropShadowEffect" />
            </Border.Effect>
        </Border>

    </Grid>
</UserControl>
