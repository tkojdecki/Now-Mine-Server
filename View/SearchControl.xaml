<UserControl x:Class="NowMine.View.SearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30">
    <Grid Margin="6, 2, 6, 2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>
        <Grid Column="0">
            <TextBox x:Name="SearchBox" Text="Type anything to search!" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" KeyDown="OnKeyDown" GotFocus="GotFocus"/>
        </Grid>
        <Grid Column="1">
            <Button x:Name="SearchButton" Content="Search!" FontSize="15" HorizontalAlignment="Center" IsEnabled="False" Click="SearchButtonClick"/>
        </Grid>
    </Grid>
</UserControl>
