<Window x:Class="NowMine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="567.006" Width="1300.344">
    <Window.Resources>


    </Window.Resources>
    <Grid>
        <WrapPanel x:Name="searchButtons">
            <TextBox x:Name="txtSearch" Text="Search" VerticalAlignment="Top" Width="120"/>
            <Button x:Name="searchButton" Content="Szukaj!" Width="75" Click="searchButton_Click"/>


        </WrapPanel>
        <ScrollViewer Margin="0,30,932,0">
            <StackPanel x:Name="searchBoard" Height="506" VerticalAlignment="Top" Width="360"/>
        </ScrollViewer>
        <WebBrowser x:Name="webPlayer" Margin="360,30,360,0"/>
        <StackPanel x:Name="queueBoard" Margin="932,30,0,10"/>
    </Grid>
</Window>
